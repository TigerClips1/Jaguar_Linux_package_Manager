ps4-convdb(8)

# NAME

ps4 convdb - convert v2 installed database to v3 format

# SYNOPSIS

*ps4 convdb*

# DESCRIPTION

*ps4 convdb* converts to current installed database to v3 format and
outputs a corresponding *installed.adb* file.

# OPTIONS

*ps4 convdb* does not support any specific options. See *ps4*(8) for global
options.
